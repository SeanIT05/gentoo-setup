# These settings were set by the catalyst build script that automatically
# built this stage.
# Please consult /usr/share/portage/config/make.conf.example for a more
# detailed example.
# Default make.conf
# Feel free to use the make.conf.lto FOR ADVANCED USAGE ONLY

COMMON_FLAGS="-march=native -O2 -pipe"
# VIDEO_CARDS=""
# INPUT_DEVICES=""
CONFIG_PROTECT="/var/bind"
PORTAGE_TMPDIR="/var/tmp"
DISTDIR="/var/gentoo/distfiles"
LINGUAS="en_US"
MAKEOPTS="-j2"

EMERGE_DEFAULT_OPTS="--verbose --keep-going --with-bdeps=y --complete-graph=y"
# PORTAGE_NICENESS="19"
CFLAGS="${COMMON_FLAGS}"
CXXFLAGS="${COMMON_FLAGS}"
FCFLAGS="${COMMON_FLAGS}"
FFLAGS="${COMMON_FLAGS}"
ACCEPT_LICENSE="*"
USE="X alsa elogind -systemd -webengine -gnome -kde"

# NOTE: This stage was built with the bindist Use flag enabled

# This sets the language of build output to English.
# Please keep this setting intact when reporting bugs.
LC_MESSAGES=C
CHOST="x86_64-gentoo-linux-musl"
ACCEPT_KEYWORDS="~amd64"
FEATURES="parallel-install candy collision-protect compressdebug compress-index parallel-fetch"

